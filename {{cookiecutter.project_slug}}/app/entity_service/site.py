from typing import List

from entity import site

class SiteListService:

    def __init__(self, *, id:int, sites: List[Site]):
        self.id = int
        self.sites = sites

    def add_site(site: Site):
        sef.sites.append(site)

    def remove_site(site: Site):
        self.sites.remove(site)
